version: 0.0.{build}
pull_requests:
  do_not_increment_build_number: true
configuration: Release
platform: x64
before_build:
- cmd: nuget restore
build:
  project: Cinecoder.Samples.sln
  verbosity: minimal
